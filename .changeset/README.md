# Changesets

User-visible changes need a changeset. Run `npm run changeset`, choose the
appropriate semantic-version bump, and describe the impact in plain language.

Changes limited to tests, internal tooling, or documentation normally do not
need a release note unless they change the contributor or consumer experience.
